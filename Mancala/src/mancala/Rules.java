package mancala;

/** Takes, sets, and enforces rules.
 * 
 * @author MancalaTeam
 */

public class Rules {
	
	/**
	 * Sets up rules for game based on given parameters.
	 * @param rows number of rows
	 * @param columns number of columns
	 * @param startBeans number of beans per hole (at start)
	 * @param sideBins whether has side bins or not
	 */
	public void Rules (int rows, int columns, int startBeans, boolean sideBins) {
		// put work here
	}
	
	/**
	 * Sets up default rules if parameters aren't given
	 */
	public void Rules () {
		Rules(5,5,5,true);
	}
	
	/**
	 * Displays tutorial?
	 */
	public void showTutorial () {
		// put work here
	}
}

